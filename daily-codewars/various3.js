function testEven(n) {
  return n % 2 === 0 ? true : false;
}

function howManyLightsabersDoYouOwn(name="Jane") {
  return name === 'Zach' ? 18 : 0
}

function move (position, roll) {
 return roll*2 + position 
  }

  function getChar(c) {
  return String.fromCharCode(c)
}